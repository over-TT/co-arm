[Unit]
# WayVNC binds only to the dedicated direct-LAN address. Wait until
# NetworkManager has actually restored that address before starting.
Wants=network-online.target
After=network-online.target

[Service]
# Avoid a tight retry loop and bound a failed stop so recovery is prompt.
RestartSec=2s
TimeoutStopSec=10s
